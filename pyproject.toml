[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "flowex"
version = "0.1.0"
description = "Go-powered real-time crypto market data for Python"
requires-python = ">=3.10"

[tool.setuptools.package-data]
flowex = ["lib/*"]
